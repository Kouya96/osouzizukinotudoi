<!--投稿編集画面-->
<div class='container'>
  <div class='row'>
    <div class="col-sm-12 mx-auto">
      <h3 class = headline>投稿編集画面</h3>
      <%= render 'layouts/errors', obj: @item %>
      <%= render 'form', item: @item %>
      <%= link_to 'Show', public_item_path(@item) %> |
      <%= link_to 'Back', public_item_path, class: "back" %>
    </div>
  </div>
</div>