<%= form_with model: comment, url: public_item_comments_path(item), local: true do |f| %>
  <%= f.text_area :comment, rows:'5',placeholder: "コメントをここに入力してください", class: "w-100" %>
  <%= f.submit "送信する", class: "btn btn-primary" %>
<% end %>